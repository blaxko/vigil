{
  "name": "vigil",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts",
    "seed:devnet": "ts-node --project tsconfig.json scripts/seed-devnet.ts",
    "demo:devnet": "ts-node --project tsconfig.json scripts/devnet-demo.ts",
    "replay:fetch": "ts-node --project tsconfig.json scripts/replay/fetch-historical-data.ts",
    "replay:run": "ts-node --project tsconfig.json scripts/replay/run-replay.ts",
    "replay:compare": "ts-node --project tsconfig.json scripts/replay/build-comparison.ts",
    "replay:verify": "ts-node --project tsconfig.json scripts/replay/verify-signatures.ts",
    "replay:stress": "ts-node --project tsconfig.json scripts/replay/stress-test.ts",
    "oracle:refresh": "ts-node --project tsconfig.json scripts/refresh-oracle.ts",
    "refill:vault": "ts-node --project tsconfig.json scripts/refill-debt-vault.ts"
  },
  "devDependencies": {
    "@coral-xyz/anchor": "^0.32.1",
    "@solana/spl-token": "^0.4.9",
    "@types/chai": "^4.3.16",
    "@types/mocha": "^10.0.7",
    "@types/node": "^20.14.9",
    "chai": "^4.4.1",
    "mocha": "^10.6.0",
    "ts-mocha": "^10.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  }
}
